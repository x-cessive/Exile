# Exile / XCSV Claude Code Adapter

**XCSV-AI-CONTRACT: 1.0.0**

Read `AI-START-HERE.md`, then both canonical XCSV policies before roadmap interpretation or implementation:

- `D:\XCSV\wiki\AI-Start-Here.md`
- `D:\XCSV\wiki\AI-Provenance-and-Doc-Sync.md`

When Architect says **read the GitHub**, **read the roadmap**, **get caught up**, **resume XCSV**, or equivalent, execute the canonical `READ_ONLY_BOOTSTRAP` first.

During bootstrap, classify desktop/GitHub documentation state as `SYNCED`, `DESKTOP_AHEAD`, `GITHUB_AHEAD`, `DIVERGED`, or `UNKNOWN`. Treat the first 2026-08-07 reconciliation as `DIVERGED` until local evidence proves otherwise. Preserve both sides; never overwrite one merely to force agreement.

Reconcile roadmap intent against this working tree, Git/GitHub, `LiveSource`, packed/deployed state and runtime evidence. Third-party catalogue presence is not proof that an addon is deployed, compatible, healthy or authorized for modification.

Any AI-authored commit must include XCSV provenance trailers. Prefer `D:\XCSV\tools\ai-commit.ps1` after reviewing/staging the intended files. Do not fake Git author identities.

Preserve licensing boundaries and never treat a packed PBO as the only source copy of live work.
